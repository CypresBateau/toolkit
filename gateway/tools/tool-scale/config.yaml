name: tool-scale
type: remote
endpoint: http://tool-scale:8000
gpu_memory_mb: 0                    # 纯 CPU，不占 GPU 显存
load_time_s: 5
call_timeout_s: 10
description: "44 个临床评分计算器（SOFA、APACHE II、ARISCAT 等），JSON 参数输入"
input: json
output: score
